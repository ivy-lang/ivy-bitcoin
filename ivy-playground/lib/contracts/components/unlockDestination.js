// external imports
import React from "react";
import { connect } from "react-redux";
const UnlockDestination = (props) => {
    return React.createElement("div", null);
};
export default connect(state => ({}) // ({ unlockInput: getClauseUnlockInput(state), contract: getSpendContract(state) })
)(UnlockDestination);
